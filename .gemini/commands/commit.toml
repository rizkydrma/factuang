description = "Generate a conventional commit message based on staged changes."

prompt = """
Review the following staged changes and generate a concise, high-quality commit message 
following the Conventional Commits specification.

Staged changes:
!{git diff --staged}

Current status:
!{git status}
"""
